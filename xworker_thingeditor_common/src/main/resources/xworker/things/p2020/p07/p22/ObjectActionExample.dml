<?xml version="1.0" encoding="utf-8"?>

<QuickContent name="ObjectActionExample" descriptors="xworker.content.QuickContent" label="对象的行为"
     type="thingDesc" group="dynamicModel.-990|Actions.util.object" groupDescriptor="true"
     icon="icons/html.png" simpleBrowser="true" th_createIndex="true" th_registThing="child|xworker.example.ExamplesIndex">
    <description><![CDATA[<h2>对象的行为示例</h2>

<p>&nbsp;&nbsp;&nbsp;&nbsp; 动作可以作为对象的行为。对象的行为可以继承和重写等。</p>

<h3>self变量</h3>

<p>&nbsp;&nbsp;&nbsp; 当动作作为对象的行为执行时，变量上下文中的self变量是对象模型自己。</p>

<p>&nbsp;&nbsp;&nbsp; 需要注意的是，如果动作是对象类的动作，那么此时self变量是动作自己，要想获取对象的变量，那么可以使用Bengin模型把对象的self变量保存成其它变量名。</p>

<h3>对象行为的搜索顺序</h3>

<p>&nbsp;&nbsp;&nbsp; 要获取一个对象的行为，首先从对象模型自身上寻找，其次是从其描述者及描述者的继承上寻找，再次是从继承者及继承者的继承上寻找。</p>
]]></description>
</QuickContent>