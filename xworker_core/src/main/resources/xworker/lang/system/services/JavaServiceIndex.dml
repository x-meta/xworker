%1565949705620
@
sname=JavaServiceIndex
slabel=JavaServiceIndex
sgroup=xworker.system.service
scontentDisplayType=Composite
scontentDefaultOpenMethod=thingDesc
Sdescription=
#$@text#$@
<p><strong>Java服务</strong></p>

<p>&nbsp;&nbsp;&nbsp; 用Java接口来定义服务，把接口的实现注册到ServiceManager中，其它应用可以通过ServiceManager获取该服务的实现。</p>

<p><strong>定义服务</strong></p>

<p>&nbsp;&nbsp;&nbsp; 定义一个Java接口。编写一个文档模型注册到这里。</p>

<p><strong>实现服务</strong></p>

<p>&nbsp;&nbsp;&nbsp; 实现Java接口，然后调用xworker.service.ServiceManager.regist(Object service, Class&lt;?&gt; serviceClass)方法注册服务。</p>

<p><strong>调用服务</strong></p>

<p>&nbsp;&nbsp;&nbsp; 使用&lt;T&gt; T xworker.service.ServiceManager.getService(Class&lt;T&gt; serviceClass)方法返回对应Java接口服务的实现。</p>

<p>&nbsp;&nbsp;&nbsp; 有可能为null，如果没有服务的实现注册了。</p>

<p>&nbsp;&nbsp;&nbsp;</p>
#$@text#$@
sdescriptors=xworker.lang.util.ThingIndex
sinheritDescription=false
sth_createIndex=true
sth_registThing=child|xworker.lang.util.ThingIndex
sth_registMyChilds=false
sth_registDisabled=false
sth_fileMonitor=false
sth_mark=false
