<?xml version='1.0' encoding='utf-8'?>

<QuickContent name="JCraftDoc" descriptors="xworker.content.QuickContent" label="jsch" type="thingDesc"
     group="xworker.jsch" groupDescriptor="true" icon="icons/html.png" simpleBrowser="true"
     th_createIndex="true" th_registThing="child|xworker.example.ExamplesIndex">&#xd;
    <description><![CDATA[<h1>xworker_jsch</h1>

<p>&nbsp;&nbsp;&nbsp; xworker_jsch模型库是对<a href="http://www.jcraft.com">www.jcraft.com</a>下面的模型库的封装，主要是封装了jsch和jcterm。</p>

<h2>jsch</h2>

<p>&nbsp;&nbsp;&nbsp; jsch是SSH的连接，可以连接到服务器执行远程命令。它们一般封装成了动作，可以在动作中使用它们。</p>

<h2>jcterm</h2>

<p>&nbsp;&nbsp;&nbsp; JCTerm是终端，设置了Sessio对象后，可以在XWorker的管理器中打开SSH终端或SFTP终端。XWorker也编写了一个单独的应用，它是<a href="javascript:invoke('shell:xworker.net.jsch.swt.JCTermWorkbench')">xworker.net.jsch.swt.JCTermWorkbench</a>。</p>

<p>&nbsp;</p>]]></description>&#xd;
    <en_description><![CDATA[<h1>xworker_jsch</h1>

<p>&nbsp;&nbsp;&nbsp; The xworker_jsch model library is an encapsulation of the model library below <a href="http://www.jcraft.com">www.jcraft.com</a>, mainly encapsulated jsch and jcterm. </p>

<h2>jsch</h2>

<p>&nbsp;&nbsp;&nbsp; jsch is an SSH connection, you can connect to the server to execute remote commands. They are generally encapsulated into actions, and they can be used in actions. </p>

<h2>jcterm</h2>

<p>&nbsp;&nbsp;&nbsp; JCTerm is the terminal. After setting the Sessio object, you can open the SSH terminal or SFTP terminal in the XWorker manager. XWorker also wrote a separate application, which is <a href="javascript:invoke('shell:xworker.net.jsch.swt.JCTermWorkbench')">xworker.net.jsch.swt.JCTermWorkbench</a>. </p>

<p>&nbsp;</p>]]></en_description>&#xd;
</QuickContent>