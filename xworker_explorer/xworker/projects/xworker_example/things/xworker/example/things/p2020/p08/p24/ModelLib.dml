<?xml version="1.0" encoding="utf-8"?>

<QuickFunction name="ModelLib" descriptors="xworker.swt.xworker.ThingRegistThing" label="模型库"
     type="thingDemo" path="xworker.example.ide.lib.ModelLib" group="dynamicModel.programming"
     icon="icons/application.png" th_sortWeight="10" th_createIndex="true" th_registThing="child|xworker.example.ExamplesIndex">
    <description><![CDATA[<h1>模型库</h1>

<h2>1.打包模型库</h2>

<p>&nbsp;&nbsp;&nbsp; 模型也可以做成库分享给其它项目。方法是把模型和相关的类打包到一个Jar中，在Jar中的根目录创建一个名为.dml的文件，内容是name=xxx，其中xxx是项目名。</p>

<p>&nbsp;&nbsp;&nbsp;&nbsp;当前示例打开的是一个导出类库的动作示例，运行后会打开保存文件对话框，如果选择了文件则会保存一个jar。这个jar设置了.dml，见最后一个叶子节点dml。</p>

<h2>2.导入模型库</h2>

<p>&nbsp;&nbsp;&nbsp; 把模型库拷贝到xworker的lib目录下就可以导入该模型库了。如果是Java项目，则同导入jar的方法。</p>
]]></description>
</QuickFunction>