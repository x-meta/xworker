<?xml version="1.0" encoding="utf-8"?>

<QuickFunction name="GlobalActionListener" descriptors="xworker.swt.xworker.ThingRegistThing"
     label="全局监听器" type="thingDesc" group="dynamicModel.-1000|Actions.listener" icon="icons/html.png"
     th_sortWeight="10" simpleBrowser="true" th_createIndex="true" th_registThing="child|xworker.example.ExamplesIndex">
    <description><![CDATA[<h1>全局监听器</h1>

<p>&nbsp;&nbsp;&nbsp; 全局监听器可以监听整个系统的动作的执行，如XWorker的调试功能，以及动作性能记录等都是通过全局监听器实现的。</p>

<p>&nbsp;&nbsp;&nbsp; 要添加一个全局监听器可以通过World对象。</p>

<pre class="brush:groovy;">
import org.xmeta.Thing;
import org.xmeta.World;

World world = World.getInstance();

//添加全局监听器
world.addGlobalActionListener(Thing listener);

//移除全局监听器
world.removeGlobalActionListener(Thing listener);</pre>

<p>&nbsp;</p>
]]></description>
</QuickFunction>