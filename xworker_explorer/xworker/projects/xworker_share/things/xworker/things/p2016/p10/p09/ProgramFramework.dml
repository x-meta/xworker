%1544575233217
@
sname=ProgramFramework
slabel=编程框架
stype=thingDesc
sgroup=-9000|编程框架.-960|动态模型.框架
sicon=icons/html.png
sstyledTextForInsert=parentContext.parentContext.codeText
Sdescription=
#$@text#$@
<h1>编程框架</h1>

<h2>1.框架模型</h2>

<p>&nbsp;&nbsp;&nbsp;&nbsp;在XWorker中框架通常是指可以扩展的模型，比如SWT的界面控件，HTML的网页控件等。使用框架后第三方可以添加新的模型，使用者也可以使用这些模型。</p>

<h2>2.定义框架</h2>

<p>&nbsp;&nbsp;&nbsp; 每一个框架都有一些约定，通常约定的是行为。比如SWT框架约定每一个SWT控件都有一个<strong>create</strong>方法，WEB应用模型都有一个<strong>httpDo</strong>方法。</p>

<p>&nbsp;&nbsp;&nbsp; 在方法的约定里，一般也同时约定了变量和内部实现等，这个要参看具体的框架。</p>

<h2>3.扩展</h2>

<p>&nbsp;&nbsp;&nbsp; 每一个框架都是可扩展的，方法是通过<strong>注册</strong>，通常的描述是以xxx注册到yyy事物下。</p>

<p>&nbsp;&nbsp;&nbsp; 比如：动作模型可以以<strong>child</strong>的方式注册到<u><strong>xworker.lang.actions.Actions</strong></u>事物下。这句话的意思是<u>xworker.lang.actions.Actions</u>是一个模型，而当前的模型想要作为动作的扩展，那么就在编辑器中选择MetaThing来编辑，并在Regist标签页中设置关联事物为<u>child|xworker.lang.actions.Actions</u>。</p>

<p>&nbsp;</p>

<p>&nbsp;</p>
#$@text#$@
sen_label=Programming Framework
sen_group=-9000|Programing.-960|Dynamic Model.Framework
sdescriptors=xworker.swt.xworker.ThingRegistThing
sinheritDescription=false
sth_createIndex=true
sth_registThing=command|xworker.command.Root
sth_registMyChilds=false
sth_registDisabled=false
Sen_description=
#$@text#$@
<h1>Programming Framework</h1>

<p>&nbsp;&nbsp;&nbsp;&nbsp;Using the model is easy to write frameworks, and XWorker also includes a large number of frameworks.</p>

<h2>1.Appointment</h2>

<p>&nbsp;&nbsp;&nbsp; Each frame has some conventions. Conventions are usually made on behavior. For example, the SWT framework specifies that each SWT control has a <strong>create </strong>method, and that the WEB application model has an <strong>httpDo </strong>method.</p>

<h2>2.Expand</h2>

<p>&nbsp;&nbsp;&nbsp; Each framework is extensible by registering, and the usual description is to register with XXX under yyy.</p>

<p>&nbsp;&nbsp;&nbsp; For example, an action model can be registered as child under <u>xworker.lang.actions.Actions</u>. This sentence means that<u> xworker.lang.actions.Actions </u>is a model, and the model of actions to expand, then select MetaThing in the editor to edit, and in the Regist tab settings associated things for <u>child|xworker.lang.actions.Actions.</u></p>
#$@text#$@
sth_mark=false
