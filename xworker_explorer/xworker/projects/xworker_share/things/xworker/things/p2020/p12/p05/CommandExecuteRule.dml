<?xml version="1.0" encoding="utf-8"?>

<QuickFunction name="CommandExecuteRule" descriptors="xworker.swt.xworker.ThingRegistThing"
     label="命令执行规则" type="thingDesc" group="command" icon="icons/html.png" simpleBrowser="true"
     en_label="Command Execute Rule" th_createIndex="true" th_registThing="command|xworker.lang.command.CommandDomain">
    <description><![CDATA[<h1>命令执行规则</h1>

<p>&nbsp;&nbsp;&nbsp; 命令相当于函数，执行时总是先执行参数的函数，如果参数的函数已经执行了，那么才执行当前命令。</p>

<p>&nbsp;&nbsp;&nbsp; 如果命令已经执行了，那么会自动回到上一级，即调用命令的参数所在的命令。如果当前命令已经是根命令了，那么整个命令执行结束。</p>
]]></description>
</QuickFunction>