<?xml version="1.0" encoding="utf-8"?>

<QuickFunction name="SaveReturnValue" descriptors="xworker.swt.xworker.ThingRegistThing" label="保存返回值"
     type="thingDemo" path="xworker.things.p2020.p08.p19.SaveReturnValue/@ActionThing"
     group="dynamicModel.-1000|Actions.managevariables" icon="icons/application.png"
     th_sortWeight="50" th_createIndex="true" th_registThing="child|xworker.example.ExamplesIndex">
    <description><![CDATA[<h1>保存返回值</h1>

<p>&nbsp;&nbsp;&nbsp; 可以动作执行后的返回值保存的动作上下文中。方法是设置动作的下列属性。</p>

<ul>
	<li><strong>varScope(保存变量范围）</strong><br />
	是保存到局部变量范围还是全局变量范围。<br />
	&nbsp;</li>
	<li><strong>saveReturn(保存返回值）</strong><br />
	只有选中了才会保存返回值。<br />
	&nbsp;</li>
	<li><strong>returnVarName(保存返回值的变量名）</strong><br />
	给一个变量名，用于保存返回值。</li>
</ul>

<p>&nbsp;&nbsp;&nbsp; 属性设置可以参看当前实例的userDir节点的Return标签页。</p>
]]></description>
    <ActionThing>
        <getUserDir name="userDir" descriptors="xworker.lang.actions.utils.SystemUtils/@getUserDir"
             saveReturn="true" returnVarName="userDir"></getUserDir>
        <Println name="Println" message="var:userDir"></Println>
    </ActionThing>
</QuickFunction>