<?xml version="1.0" encoding="utf-8"?>

<QuickFunction name="MappingIntroduce" descriptors="xworker.swt.xworker.ThingRegistThing" label="映射"
     type="thingDesc" group="-6000|数据管理.-6000|数据对象.-10000|映射" groupDescriptor="true"
     simpleBrowser="true" en_group="-6000|Data.-6000|DataObject.-10000|Mapping" th_createIndex="true"
     th_registThing="child|xworker.doc.DocIndex">
    <description><![CDATA[<h1>映射</h1>

<p>&nbsp;&nbsp;&nbsp; 数据对象可以映射各种数据类型，以实现对这些类型的数据的增删改查操作。比如可以映射数据库、Excel等等。</p>

<h2>1.基本知识</h2>

<p>&nbsp;&nbsp;&nbsp; 数据对象是一个Map，一个数据对象可以映射数据库中的一张表，或Excel的一个Sheet等。</p>

<p>&nbsp;&nbsp;&nbsp; 映射主要是设置数据对象的属性，比如在映射数据库的表时，是把数据库表的字段映射到数据对象的属性中。</p>

<h2>2.简单映射</h2>

<p>&nbsp;&nbsp;&nbsp; 数据对象只能做一些简单映射，比如一个数据对象只能映射数据库的一张表或视图。</p>

<h2>3.单主键</h2>

<p>&nbsp;&nbsp;&nbsp; 由于一些界面在更新数据时只支持单主键的数据对象，因此建议一个数据对象只设置一个主键。</p>
]]></description>
</QuickFunction>